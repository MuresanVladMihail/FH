# Test that stacktraces are generated (not empty)
# We can't test exact format, but we can verify errors produce output

fn cause_error() {
    let x = 10;
    let y = 0;
    return x / y;  # Division by zero
}

fn level2() {
    return cause_error();
}

fn level1() {
    return level2();
}

fn test_stacktrace_exists() {
    # We expect this to fail, so we can't run it directly
    # Just verify the structure is sound
    return true;
}

fn main() {
    # Test that error handling works
    # (Can't actually trigger error and continue in FH)
    assert(test_stacktrace_exists());

    print("ok\n");
}
