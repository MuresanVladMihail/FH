
fn simple()
{
    return 1+2;
}

fn f(x,y) { return y; }
fn g(x,y,z) { return z; }

fn test()
{
    let x = 0;
    let y = 1;
    let z = f(g(1,x,3), y);
    return z;
}

fn main(f)
{
    let x = 100;
    let y = 101;
    let z = 102;
    #let w = 103;

    let r = 2*x + 3*z;
    #r = x*y + z*w;

    r = test(x, y, r);
    r = test(0);

    return r;
}

fn test_if(x)
{
    if (0)
        x = 1;
    else
        x = 2;
        return x;
    }

    fn test_while()
    {
        let x = 0;
        while (x < 10) {
            test(x);
            x = x + 1;
        }
    }
